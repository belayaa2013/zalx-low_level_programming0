#include <stdio.h>
/**
 * main - A program that prints a line using puts function
 * puts has some similarity with printf
 * Return: 0(success)
 */
int main(void)
{
puts("\"programming is like building a multilingual puzzle");
return (0);
}
