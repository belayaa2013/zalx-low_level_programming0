#include <stdio.h>
/**
 * main - a program that prints a line using puts function
 * puts and printf can function in some cases
 * Return: 0(success)
 */
int main(void)
{
puts("\"programming is like building a multilingual puzzle");
return (0);
}
